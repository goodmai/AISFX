# TODO: Улучшения для Scala AI Platform (AISFX v2 Standards Applied)

## 0. Стандарты и Качество Кода (Общие Задачи)

0.1. **[AISFX] Аудит и Предотвращение Циклических CSS Стилей**
* **Описание:** Провести аудит существующих CSS файлов (`main.css`, `header.css`, `footer.css`, `history_panel.css`, `response_area.css`) на предмет циклических зависимостей, излишне сложных селекторов или практик, которые могут привести к проблемам с производительностью или переопределением стилей. Внедрить запрет на использование таких практик в дальнейшем.
* **AISFX Принцип:** Поддерживаемость, чистота кодовой базы, использование CSS для стилизации (эффективное).
* **Действие:**
* [ ] Проанализировать структуру CSS файлов и `@import` (если используются) на предмет циклов.
* [ ] Оценить сложность селекторов; упростить, где возможно, для повышения производительности и читаемости.
* [ ] Документировать лучшие практики по написанию CSS для проекта.
* **Компоненты:** Все `.css` файлы.
* **Сложность:** Средняя.

0.2. **[AISFX] Соответствие Стандартам Разработки**
* **Описание:** При выполнении всех нижеследующих задач, строго придерживаться "Стандартов Разработки и Лучших Практик (AISFX)", включая:
* Неизменяемость (Immutability).
* Чистые функции (где применимо).
* Модульность и разделение ответственности (MVC-подобная архитектура, SRP).
* Использование `Try`, `Either`, `Option` для обработки ошибок.
* Корректное использование асинхронности (`Future`, Pekko Actors).
* Структура проекта, именование, ограничения на размер класса, документирование (Scaladoc).
* Принципы UI (ScalaFX): разделение логики, CSS для стилей, отзывчивость, байндинги.
* Централизованное управление состоянием (`StateManager`, `AppState`).
* **Действие:** Постоянный контроль при разработке и ревью кода.
* **Компоненты:** Весь проект.
* **Сложность:** Постоянная задача.

## I. Критические Задачи и UI/UX Исправления

1.  **[БАГ/ДИАГНОСТИКА] Решить проблему с отображением кнопок-иконок в футере (➕, 📎, 📁) – Альтернативный Подход**
  * **Описание:** Текущий подход с `Text` нодами для Emoji нестабилен. Реализовать отображение иконок с использованием SVG или качественных растровых изображений.
  * **AISFX Принцип:** "Использовать CSS для стилизации. Минимизировать использование инлайн-стилей." (Применимо к SVG). "Модульность" (новый `IconUtil.scala`).
  * **Предлагаемые альтернативные подходы (выбрать один):**
    * **1. SVG Иконки (Предпочтительно):**
      * [ ] Найти/создать SVG-определения для иконок (плюс, скрепка, папка).
      * [ ] Создать `IconUtil.scala` для генерации `SVGPath` или `Region` с `SVGPath`. Логика должна быть чистой функцией, если возможно.
      * [ ] Заменить вызовы `EmojiUtil.createEmojiText` в `Footer.scala` на вызовы из `IconUtil`.
      * [ ] Стилизовать SVG (цвет, размер при наведении) через CSS-классы, присваиваемые `SVGPath` или кнопке-контейнеру.
    * **2. Растровые Изображения (PNG):**
      * [ ] Подготовить PNG файлы иконок (например, 16x16 или 24x24).
      * [ ] Поместить в `src/main/resources/icons/`.
      * [ ] `IconUtil.scala` будет загружать `Image` и создавать `ImageView`.
      * [ ] Обновить `Footer.scala`. Стилизация состояний (например, disabled) может потребовать смены `ImageView.image` или CSS `opacity`.
  * **Обоснование выбора подхода:** SVG предпочтительнее из-за масштабируемости без потерь и гибкости стилизации через CSS. PNG проще на начальном этапе, если нет готовых SVG.
  * **Компоненты:** `Footer.scala`, `EmojiUtil.scala` (будет заменен или дополнен `IconUtil.scala`), `footer.css`, `main.css`, новые ресурсы в `icons/`.
  * **Сложность:** Средняя.

2.  **[УЛУЧШЕНИЕ UI] Оптимизация синхронизации UI и выбора топика в `HistoryPanel`**
  * **Проблема:** Мерцание и нежелательные промежуточные состояния UI при смене топика.
  * **AISFX Принцип:** "Обеспечивать отзывчивость UI", "Избегать размещения сложной бизнес-логики непосредственно в классах View".
  * **Действие:**
    * [ ] Рефакторинг `MainController.setActiveTopic`, `MainController.synchronizeUIState`.
    * [ ] Усилить контроль над `isProgrammaticSelectionFlag`.
    * [ ] Оптимизировать обновления `ResponseArea`, избегая ненужных `clearDialog`.
  * **Компоненты:** `MainController.scala`, `HistoryPanel.scala`, `ResponseArea.scala`.
  * **Сложность:** Средняя.

## II. Расширенное Управление Контекстом Файлов

3.  **[ФУНКЦИОНАЛ] Интерактивное Дерево Файлов и Каталогов**
  * **Задача:** Отображение иерархической структуры файлов с возможностью выбора.
  * **AISFX Принцип:** "Модульность" (`FileTreeView.scala`), "Разделение Ответственности" (`FileManager.scala` - логика, `FileTreeView.scala` - отображение).
  * **Требования:**
    * [ ] UI: `TreeView` в отдельной панели.
    * [ ] Функционал: Разворачивание/сворачивание, иконки (SVG/PNG), чекбоксы для выбора, кнопки "Открыть каталог...", "Обновить", "Очистить выбор".
  * **Проверка реализации:** Базовые классы (`FileNode.scala`, `FileTreeView.scala`, `FileManager.scala`) есть. Требуется интеграция в UI макет `MainApp` и тестирование.
  * **Компоненты:** `FileNode.scala`, `FileTreeView.scala`, `FileManager.scala`, `MainController.scala`.
  * **Сложность:** Интеграция и доработка - Средняя.

4.  **[ФУНКЦИОНАЛ] Отображение и Использование Содержимого Выбранных Файлов из Дерева**
  * **Задача:** Просмотр и использование содержимого файлов как контекста.
  * **Требования:**
    * [ ] Предпросмотр содержимого файла при выборе (опционально).
    * [ ] Кнопка "Добавить контекст отмеченных" в `FileTreeView`.
    * [ ] Добавление контекста в `inputTextArea` в `Footer`.
  * **Компоненты:** `FileTreeView.scala`, `FileManager.scala`, `MainController.scala`, `Footer.scala`.
  * **Сложность:** Реализовано частично, тестирование, UI предпросмотра.

5.  **[ФУНКЦИОНАЛ] Игнорирование Файлов и Каталогов в Дереве**
  * **Задача:** Исключение ненужных элементов из дерева и контекста.
  * **Требования:**
    * [ ] Список исключений по умолчанию в `FileManager.scala`.
    * [ ] Поддержка пользовательских правил (например, из `.aiplatformignore` или UI настроек).
  * **Компоненты:** `FileManager.scala`, `FileTreeView.scala`, `SettingsView.scala`.
  * **Сложность:** Расширение на пользовательские правила - Средняя.

6.  **[ФУНКЦИОНАЛ] Сжатие Выбранных Файлов/Папок в ZIP (из Дерева)**
  * **Описание:** Создание ZIP-архива из элементов, выбранных в `FileTreeView`.
  * **AISFX Принцип:** "Использовать сервисы (`service/`) для взаимодействия с файловой системой" (`FileManager`).
  * **Требования:**
    * [ ] Действие/кнопка "Создать ZIP из отмеченного".
    * [ ] Диалог сохранения ZIP. Использование `java.util.zip`.
  * **Компоненты:** `FileTreeView.scala`, `FileManager.scala`.
  * **Сложность:** Средняя/Высокая.

## III. Улучшения Логики Приложения и Функционала

7.  **[УЛУЧШЕНИЕ] Блокировка Повторного Запроса**
  * **Требование:** Запретить отправку нового запроса, пока не получен ответ на предыдущий.
  * **Проверка реализации:** `MainController.isRequestInProgress`, `Footer.setLocked(boolean)` есть.
  * **Действие:** [ ] Тщательная проверка корректной установки флага и блокировки всех релевантных UI элементов.
  * **Компоненты:** `MainController.scala`, `Footer.scala`.
  * **Сложность:** Низкая (проверка).

8.  **[ФУНКЦИОНАЛ] Автоматическое Обновление Заголовка Топика**
  * **Требование:** Автогенерация заголовка для новых/пустых топиков после первого запроса.
  * **Проверка реализации:** `TopicManager.generateAndSetTopicTitle` есть.
  * **Действие:** [ ] Убедиться в корректности определения "нового/пустого" топика и однократности вызова. [ ] Гарантировать плавное обновление UI в `HistoryPanel`.
  * **Компоненты:** `RequestExecutionManager.scala`, `TopicManager.scala`, `MainController.scala`, `HistoryPanel.scala`.
  * **Сложность:** Низкая/Средняя (проверка и отладка).

9.  **[ФУНКЦИОНАЛ] Гибкая Логика Пайплайнов Обработки Запросов**
  * **Задача:** Внедрение настраиваемых "пайплайнов" обработки AI-запросов.
  * **AISFX Принцип:** "Модульность и Разделение Ответственности", "Использовать менеджеры (`manager/`) для инкапсуляции сложной логики".
  * **Требования:**
    * [ ] Модели: `Pipeline.scala`, `PipelineStep.scala`.
    * [ ] Конфигурация UI: В `SettingsView.scala`.
    * [ ] Исполнение: Рефакторинг `RequestExecutionManager.scala`.
  * **Компоненты:** Новые классы, `RequestExecutionManager.scala`, `PresetManager.scala`, `AppState.scala`, `SettingsView.scala`, `MainController.scala`.
  * **Сложность:** Высокая.

10. **[ФУНКЦИОНАЛ] Реализовать Вкладку "Горячие Клавиши" в Настройках**
  * **Описание:** UI для настройки горячих клавиш.
  * **AISFX Принцип:** "Управление Состоянием" (хранение настроек клавиш).
  * **Требования:**
    * [ ] Определение действий для биндинга.
    * [ ] Хранение конфигурации (в `AppState` или отдельно).
    * [ ] UI в `SettingsView` для редактирования.
    * [ ] Регистрация обработчиков в `MainApp` или `MainController`.
  * **Компоненты:** `SettingsView.scala`, `MainController.scala`, `AppState.scala`, `MainApp.scala`.
  * **Сложность:** Высокая.

## IV. Рефакторинг, Верификация и Прочие Задачи (AISFX Compliance)

11. **[РЕФАКТОРИНГ] Рефакторинг `SettingsView.scala`**
  * **Описание:** Декомпозиция `SettingsView.scala` для соблюдения ограничения на размер класса (<500 строк) и улучшения модульности.
  * **AISFX Принцип:** "Размер Класса", "Модульность".
  * **Действие:** [ ] Вынести логику редактора пресетов и маппинга кнопок в отдельные классы-хелперы или вложенные UI-компоненты.
  * **Компоненты:** `SettingsView.scala`.
  * **Сложность:** Средняя.

12. **[УЛУЧШЕНИЕ] Обработка Ошибок UI и Логирование**
  * **Описание:** Улучшение системы логирования и отображения ошибок в UI.
  * **AISFX Принцип:** "Использовать `Try`, `Either` или `Option`", "Предоставлять информативные сообщения об ошибках", "Логировать ошибки с достаточным контекстом".
  * **Действие:**
    * [ ] Расширить `DEBUG/INFO` логирование для ключевых UI событий.
    * [ ] Для некритических ошибок UI (загрузка CSS, моделей) использовать `DialogUtils.showWarning` или ненавязчивые уведомления.
  * **Компоненты:** `MainApp.scala`, `MainController.scala`, все компоненты View.
  * **Сложность:** Средняя.

13. **[ВЕРИФИКАЦИЯ] Корректность Работы Режима "Global"**
  * **Задача:** Убедиться, что режим "Global" корректно обрабатывается (запросы без шаблонов пресетов).
  * **Проверка реализации:** Логика присутствует в `RequestExecutionManager.prepareRequestData`.
  * **Действие:** [ ] Провести тестирование сценария "Global". [ ] Проверить логи для подтверждения корректной обработки.
  * **Компоненты:** `MainController.scala`, `RequestExecutionManager.scala`.
  * **Сложность:** Низкая (тестирование).

14. **[СТИЛИ/ВЕРИФИКАЦИЯ] Стилизация Блоков Кода в `ResponseArea`**
  * **Задача:** Визуальная проверка отображения блоков кода.
  * **Проверка реализации:** `ResponseArea.createCodeBlockNode` и `response_area.css` отвечают за это.
  * **Действие:** [ ] Протестировать отображение блоков кода. [ ] Проверить кнопку "Copy".
  * **Компоненты:** `ResponseArea.scala`, `response_area.css`.
  * **Сложность:** Низкая (тестирование).